version: '3.8'

services:
  api:
    build: ./api
    ports:
      - "3004:3004"
    environment:
      - PORT=3004

  web:
    build: ./frontend
    ports:
      - "3005:3005"
    environment:
      - PORT=3005
      - NUXT_PUBLIC_API_BASE_URL=http://api:3004
    depends_on:
      - api